/**
 * Professional performance metrics and achievement statistics used for summary stats in profile sections.
 */
export type Stats = {
  experience: number;
  services: number;
  projects: number;
  achievements: number;
  articles: number;
};